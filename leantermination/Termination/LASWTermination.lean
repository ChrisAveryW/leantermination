import leantermination.Datastructures.IntegerProgram
import leantermination.Termination.AcyclicIntegerProgram

/-
theorem bounded_path_length
    (ip : IntegerProgram)
    (hacyclic : ip.isAcyclicUpToSelfLoops)
    (hranking : ∀ t ∈ ip.self_loops, t.hasRankingFunction) :
    paths are bounded := by
  sorry-/
